import { SpyOnInitDirective } from './spy-on-init.directive';

describe('SpyOnInitDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyOnInitDirective();
    expect(directive).toBeTruthy();
  });
});

import { OrderRequestOptions } from './order-request-options';

describe('OrderRequestOptions', () => {
  it('should create an instance', () => {
    expect(new OrderRequestOptions()).toBeTruthy();
  });
});

import { ApproverHierarchyPipe } from './approver-hierarchy.pipe';

describe('ApproverHierarchyPipe', () => {
  it('create an instance', () => {
    const pipe = new ApproverHierarchyPipe();
    expect(pipe).toBeTruthy();
  });
});

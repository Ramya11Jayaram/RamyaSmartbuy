import { InboxPorEntryPipe } from './inbox-por-entry.pipe';

describe('InboxPorEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new InboxPorEntryPipe();
    expect(pipe).toBeTruthy();
  });
});

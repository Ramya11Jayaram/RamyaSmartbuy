import { BudgetHierarchyPipe } from './budget-hierarchy.pipe';

describe('BudgetHierarchyPipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetHierarchyPipe();
    expect(pipe).toBeTruthy();
  });
});

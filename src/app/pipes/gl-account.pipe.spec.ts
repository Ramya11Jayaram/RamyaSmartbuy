import { GlAccountPipe } from './gl-account.pipe';

describe('GlAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new GlAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
